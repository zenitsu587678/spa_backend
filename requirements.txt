asgiref==3.10.0
Django>=5.2
gunicorn==23.0.0
python-dotenv==1.1.1
psycopg2-binary==2.9.12
whitenoise==6.12.0
dj-database-url==3.1.2

# Used by bookings/services.py (contact-form intent detection) and by
# my_agent/agent.py (the chat agent). google-adk pulls in google-genai
# itself, but pinning google-genai directly keeps the contact-form flow
# working even if someone only installs the web app, not the chat agent.
google-genai>=0.3.0
google-adk>=0.1.0